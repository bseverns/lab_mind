# Lab Placeholders

This folder holds editable examples for the current lab architecture.

Use these files as starting points for local copies:

- `hostnames.example`
- `service-urls.example`
- `headscale-values.example`
- `machine-inventory.example.csv`
- `digital-factory-workspace.example.md`
- `model-values.example`

Rules:

- Keep secrets out of Git.
- Keep values editable.
- Prefer placeholders over guessed production values.

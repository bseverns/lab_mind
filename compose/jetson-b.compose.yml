# Legacy edge-support example.
# Node-RED now belongs on the R900 if the lab uses it.

services:
  status-page:
    image: caddy:2
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - /srv/labbrain/status:/usr/share/caddy:ro
